# Pages 150-199 Notes

## Chapters/Sections Covered
- **Chapter 5 (continued)**: Extending Pattern Directed Inference Systems
  - 5.3 Examples (p.151)
  - 5.3.1 Example: The N-queens puzzle (p.151)
  - 5.3.2 Example: A full implementation of KM* (p.155)
  - 5.4 Reprise (p.160)
  - 5.5 Backpointers (p.161)
  - 5.6 Exercises (p.161)
  - 5.7 Bibliography (p.166)
- **Chapter 6**: Introduction to Truth Maintenance Systems
  - 6.1 Why use a TMS? (p.168)
  - 6.1.1 Identifying responsibility for conclusions (p.168)
  - 6.1.2 Recovering from inconsistencies (p.168)
  - 6.1.3 Maintaining a cache of inferences (p.169)
  - 6.1.4 Guiding backtracking (p.172)
  - 6.1.5 Default reasoning (p.172)
  - 6.2 What is a TMS? (p.173)
  - 6.2.1 How do TMSs help? (p.174)
  - 6.2.2 When are TMSs useful? (p.174)
  - 6.3 The basics of truth maintenance systems (p.175)
  - 6.3.1 Establishing a common vocabulary between the inference engine and the TMS (p.175)
  - 6.3.2 Node properties (p.176)
  - 6.3.3 Justifications (p.177)
  - 6.3.4 Labels (p.179)
  - 6.3.5 Representing TMS data (p.179)
  - 6.3.6 Basic TMS-inference engine transactions (p.180)
  - 6.4 How justifications help (p.180)
  - 6.4.1 Identifying responsibility for conclusions (p.180)
  - 6.4.2 Guiding backtracking with a TMS (p.181)
  - 6.5 The propositional specification of a TMS (p.182)
  - 6.6 Families of TMSs (p.183)
  - 6.7 Exercises (p.185)
  - 6.8 Bibliography (p.186)
- **Chapter 7**: Justification-Based Truth Maintenance Systems
  - 7.1 JTMS node properties (p.187)
  - 7.2 The propositional specification of a JTMS (p.188)
  - 7.3 Well founded support (p.189)
  - 7.4 In and out versus true and false (p.190)
  - 7.5 How justifications save inference engine work (p.190)
  - 7.6 How justifications enable default reasoning (p.192)
  - 7.7 The JTMS interface (p.193)
  - 7.8 Simple example of JTMS usage (p.197)
  - 7.9 The JTMS algorithms (p.198)
  - 7.9.1 Enabling an assumption (p.199)

## Key Definitions

- **Choice set**: Represents some factor or aspect which must appear in any complete solution. Within each choice set, the choices are mutually exclusive and collectively exhaustive. The collection of choice sets must span the set of possible solutions; that is, every solution can be found by a consistent set of selections, one from each choice set. *(p.152)*

- **Chronological search (abstract)**: Once we have broken a problem down into a collection of choice sets, one way to solve it is by chronological search: `(defun Chronos (choice-sets) (if (null choice-sets) (record-solution) (dolist (choice (first choice-sets)) (while-assuming choice (if (consistent?) (Chronos (rest choice-sets)))))))` where `record-solution` is executed whenever a consistent set of choices has been made; `while-assuming` makes the particular choice, retracting it when its body is finished executing; `consistent?` tests the choices so far for consistency. *(p.153)*

- **Contradiction (in context mechanism)**: A contradiction is any node whose datum the TMS uses the label to store its representation of the current belief in the node. A node is a contradiction if the inference engine has specified that some nodes are premises, assumptions, and contradictions. *(p.177)*

- **Premise**: A node is a premise if the inference engine has explicitly indicated that it holds universally. *(p.176)*

- **Contradiction node**: A node is a contradiction if the inference engine has indicated that it can never hold. Contradictions can never be changed. In most TMSs, when a contradiction is discovered, the inference engine is interrupted. *(p.176)*

- **Assumption**: A node is an assumption if the inference engine has explicitly designated it so. An assumption is enabled if the inference engine has signed the TMS that it chooses to believe it. Otherwise the assumption is retracted and treated as any other node -- i.e., it will be believed only if it has a valid justification. A node which is both an assumption and a premise is retracted and believed. *(p.176-177)*

- **Justification**: A justification consists of three parts: the consequent (the node of the inference engine datum which was inferred), the antecedents (the nodes of the data used as antecedents to the inference rule), and the informant (supplied by the inference engine to explain the inference in more detail). Justifications are typically written as: `((consequent) (informant) (antecedents))` *(p.177)*

- **Dependency network**: A set of nodes and justifications form a dependency network. *(p.178)*

- **Label**: All TMS implementations associate a label with each node. The TMS uses the label to store its representation of the current belief in the node. There is no hard and fast rule about what this label looks like -- each TMS family represents labels differently. For example, in the simpler TMSs the label is either :IN or :OUT. A node is labeled :OUT unless one can construct an argument using the dependency network starting from premises and assumptions leading to belief in the node. *(p.179)*

- **TMS data fields per node**: datum (supplied by inference engine), label (current belief status), justifications (set which could provide support), consequences (set of justifications which use this node as antecedent), contradictory? (if non-nil, indicates belief in this node represents a contradiction), assumption? (if non-nil, node should be treated as assumption whose belief can be explicitly enabled and retracted), premise? (if non-nil, indicates this node is a premise). *(p.179)*

- **Well-founded support**: A JTMS only determines which nodes are in and which are out, but it must also provide good explanations, or well founded support for why nodes are believed. Well founded support for node n is a sequence of justifications J1, ..., Jk which has the following properties: J1 justifies node n; all the antecedents of Ji are justified earlier in the sequence or are enabled assumptions; no node has more than one justification in the sequence. A node has a well-founded support exactly when it is propositionally derivable from the current JTMS state. *(p.189)*

- **JTMS node properties**: A node is a premise if the inference engine has provided it with a justification with no antecedents. Premises are therefore not distinguished from other nodes in the architecture. A node is a contradiction if the inference engine has explicitly designated it so. Believing a contradiction has no effect on the JTMS operations. The JTMS will simply inform the inference engine when a contradictory node becomes believed. A node is an assumption if the inference engine has explicitly designated it so. *(p.187)*

- **Three JTMS algorithms**: (1) The *extent* approach: The JTMS maintains an explicit set of nodes currently believed with their supporting justifications. (2) The *lazy* approach: The JTMS computes the belief status of a node when it is requested by the inference engine. (3) The *labeling* approach: The JTMS augments the node datastructure with two fields indicating its belief status and supporting justification. *(p.198)*

## Algorithms and Pseudocode

- **`generate-match-body`**: After sorting out the results of the analysis, `generate-match-body` finally returns the combination of the tests on the structure and the equality tests as the list of tests that must be performed on an assertion to determine if it matches, and binding specs as the expressions that will access the new bindings for the trigger's free variables. Notice again that order is important: tests that ensure a particular piece of list structure exist must be carried out before others that presume that list structure is there. That is why the equality checks are placed after every other structural test. This procedure and `generate-unify-tests` were written together to ensure that the order of the tests meshed would come out correctly with minimal crossing. *(p.150)*

- **N-Queens puzzle solution using FTRE**: The first step in instantiating this abstract procedure is to choose a representation for the queens. The assertion `(queen N T)` indicates that the queen of row N is placed in column T. The routine `queens-okay?` uses this encoding to determine whether the coordinates of two queens imply the potential for capture. This routine is used by the FTRE rule in `fcpn-ex.lisp` to assert the symbol `Contradiction` if two queens are ever discovered to be in mutual peril. Given N, the routine `n-queens` finds all consistent solutions. It begins by calling `setup-queens-puzzle`, which creates an FTRE and initializes `*r-assumptions*` to keep statistics and `*placements*` to store solutions. *(p.153-154)*

- **`solve-queens-puzzle`**: Implements the search. The procedure `make-queens-choice-sets` creates the choice sets which are then passed to `solve-queens-puzzle`. `make-queens-choice-sets` is straightforward, composing a list of choice sets, each encoded by a list of patterns involving a row's queen. `solve-queens-puzzle` does the real work by checking if the last placement led to a capture by seeing if contradiction is believed. Notice that on the first call to `solve-queens` we start with an empty board so no capture can occur. If the last placement led to a capture, we simply return without doing any further work. Otherwise, `solve-queens-puzzle` checks to see if any choice sets remain. If there are more choices to be made, the first choice set is selected and each alternative is tried in turn via the FTRE `set-to-recover` mechanism, which invokes another call to `solve-queens` with the rest of the choice sets. *(p.154)*

- **Chronological backtracking strategy**: This strategy is called chronological backtracking. It is called this because when an inconsistency is found the program backs up to the very last choice that was made. We were careful in this system to ensure that contradictions would be detected as soon as possible, so in fact the last choice was indeed relevant. As we see later, this is not always easy to do. *(p.154)*

- **Indirect proof rule for FTRE**: The contradiction rule gives us a means of detecting when an indirect proof succeeds. We need to define a rule which (1) detects when an indirect proof is an appropriate technique to try and (2) sets up a temporary context in which to try it. The latter is easy; we can simply call `assume-in-context` with the negation of the fact to be shown as the assumption and contradiction as our goal. *(p.157-158)*

- **Indirect proof via BOUNCE PROOF**: `(a-rule ((show ?p)) (unless (or (fetch ?p) (eq ?p 'contradiction) (not (simple-proposition? ?p))) (when (seek-in-context (list ,?p 'contradiction) (assert! ?p))))` This rule relies on our rule for `do` EXTENSIONS, defined in Chapter 4, to decompose the disjunctive goal and express interest in its parts. *(p.158-159)*

- **NEGATION INTRODUCTION rule**: `(a-rule ((show (not ?p)) (unless (or (fetch (not ,?p)) (when (seek-in-context (list ,?p 'contradiction) (assert! (not ?p)))))` Here restricting ?p to simple propositions doesn't make sense, since the assumption will (presumably) be a positive term which might indeed be proven by some other rule. *(p.158)*

- **Combined introduction for BOUNCE PROOF and NEGATION INTRODUCTION**: `(a-rule ((show (implies ?p ?q))) (unless (fetch (implies ,?p ,?q)) (when (seek-in-context (list ,?p) ;; Assume the antecedent (or ,?q 'contradiction)) ;; And look for consequent (assert! (implies ?p ?q))))` *(p.159)*

- **`create-jtms` interface**: `(create-jtms title &key (node-string 'default-node-string) (debugging nil) (checking-contradictions t) (contradiction-handler 'ask-user-handler) (enqueue-procedure nil))` The JTMS supplies this procedure to create a new TMS. It returns a datastructure which contains the entire state of the JTMS. *(p.194)*

- **`change-jtms` procedure**: `(change-jtms jtms &key node-string debugging checking-contradictions contradiction-handler enqueue-procedure)` Allows the inference engine to change the initial settings provided in the original call to `create-jtms`. *(p.195)*

- **`tms-create-node` procedure**: `(tms-create-node jtms datum &key assumptionp contradictoryp)` The inference engine creates a TMS node with this procedure call specifying the initial properties of the node to be created. *(p.195)*

- **JTMS node access procedures**: `(enable-assumption node)`, `(retract-assumption node)`, `(make-contradiction node)`, `(assume-node node)`, `(in-node? node)`, `(out-node? node)`, `(justify-node informant consequence antecedents)` *(p.195-196)*

- **`supporting-justification-for-node` procedure**: Returns one of three values: :ENABLED-ASSUMPTION if the node is in as an enabled assumption, or the justification currently providing support, otherwise returns nil. *(p.196)*

- **`assumptions-of-node` procedure**: Returns the set of enabled assumptions underlying the well-founded support for a node. The most common reason for exploring the well-founded explanation for a node is to identify the enabled assumptions which underlie it. This is very important for handling contradictions, where the inference engine needs to find an assumption to retract in order to remove the current inconsistency. *(p.196)*

- **Simple JTMS usage example (p.197)**: Create a fresh JTMS, create initial assumptions and enable them: `(setq assumption-a (tms-create-node *jtms* "A" :ASSUMPTIONP t))` etc. for A, B, C, D. Enable assumptions, create node k, justify it: `(setq node-k (tms-create-node *jtms* "K"))`, `(justify-node "R1" node-k (list assumption-a assumption-c))`. Then introduce node g, justify it, and then contradict it. The JTMS detects the contradiction and uncovers its underlying assumptions. *(p.197-198)*

- **Enabling an assumption (JTMS algorithm)**: Enabling an assumption is the simplest core JTMS operation. Note that assumptions which have become premises will not be enabled. First, the JTMS marks the assumption as enabled, removes its current supporting justification (if any), and marks it as supported by virtue of being an enabled assumption. Second, it checks whether the assumption was already in; if so, it does nothing more. Third, it looks to see whether any justification in which it appears as an antecedent is satisfied, i.e., that all the antecedents of the justification are labeled in. If any such justification is satisfied, it labels the consequence in and makes the justification be the supporting justification for the consequence. If any node becomes labeled in by this procedure, its consequences are recursively checked. As this algorithm only changes node labels from out to in, and as there are a finite number of nodes, it must terminate. *(p.199)*

## Data Structures

- **FTRE performance table (N-queens)**: Performance of FTRE on N queens. Times are for an IBM RT model 125, running Lucid Common Lisp. N=4: 9 rules, 66 assumptions, 0.53s. N=5: 16 rules, 228 assumptions, 1.33s. N=6: 25 rules, 616 assumptions, 6.13s. N=7: 40 rules, 1544 assumptions, 20.21s. N=8: 92 rules, 11226 assumptions, 99.53s. *(p.154)*

- **TMS node structure**: Each node has: datum, label, justifications, consequences, contradictory?, assumption?, premise?. *(p.179)*

- **Dependency network graphical notation**: Justification nodes shown with triangles pointing down, assumption nodes shown as enabled/retracted, simple nodes, premise nodes, contradiction nodes. Nodes which are both premises and contradictions (admittedly rare) are represented by superimposing their icons. *(p.178)*

- **JTMS node data structure**: The JTMS augments each node with two additional fields: its belief status (label) and supporting justification. *(p.198)*

- **Families of TMSs (2D classification)**: The vertical dimension concerns the type of constraint that the inference engine is permitted to express among the nodes. The horizontal dimension is the kind of query the TMS is expected to answer efficiently. Along the label dimension: Simple vs Complex. Along the constraint dimension: Horn/definite vs Clause. This gives four families: JTMS (simple label, Horn), ATMS (complex label, Horn), LTMS (simple label, clause), CMS (complex label, clause). *(p.184)*

## Equations

- **Propositional specification of a TMS**: Every TMS node can be interpreted as a propositional symbol. Every justification can be interpreted as a simple propositional definite clause. If nodes n1, ..., nk justify node n, then this is represented by the clause: `n1 ^ ... ^ nk -> n`, which is equivalent to the material implication. *(p.182-183)*

- **Propositional specification of a JTMS**: The basic JTMS specification can be given in terms of two sets: the set of justifications and the set of enabled assumptions. The JTMS algorithms must answer queries correctly with respect to the contents of these two sets at the moment the query is made. The set of justifications grows monotonically as we do not allow justifications to be removed. However, the set of enabled assumptions is always in flux. It is the fact that assumptions might be retracted that introduces most of the complexity in the JTMS algorithms. *(p.188-189)*

## Key Claims and Properties

- **Exponential growth in N-queens**: The curve is clearly an exponential, as one might expect from a combinatorial search problem. Even in this simple puzzle, the nemesis of all problem-solvers -- combinatorial explosion -- is apparent. In later chapters we see how dependency-directed search allows one to ameliorate this problem. In N queens, the exponential is inherent in the problem; there are simply an exponential number of solutions. In other problems, even when there is a single solution it can take exponential work to find it. No code optimization, no faster clock rate, no increment of parallel processing will overcome the computational demands of an exponential procedure in the long run. The best you can do is figure out how to formulate your problem so that you either eliminate the exponential (perhaps by settling for approximate solutions) or keep its growth as small as possible. *(p.155)*

- **Five general shortcomings addressed by a TMS**: (1) The problem solver must identify responsibility for its conclusions by providing rational explanations. (2) The problem solver must recover from inconsistencies. (3) The justifications cache information about what doesn't work as well as what does work, thus providing a cache that helps control the search. (4) The justifications pinpoint the assumptions underlying contradictions, and therefore identify the optimal backtrack point. (5) Since justifications can include explicit assumptions, the problem solver can use the TMS to manipulate assumptions and to do default reasoning. *(p.174)*

- **Problem solver = inference engine + TMS**: The fundamental architectural observation is that the problem solver can be decomposed into two parts: an inference engine and a TMS. This natural partitioning of concerns allows the inference engine to focus on drawing inferences within the task domain and the TMS to focus on beliefs, assumptions, and contexts (section 6.3.6 lists the possible transactions). The result is a much simpler and more efficient problem solver. *(p.173-174)*

- **When TMSs are NOT useful**: A fundamental assumption of a TMS is that consulting a set of cached inferences is more efficient than rerunning the inference rules that generated them in the first place. However, if the inference rules are very inexpensive and the task does not require an exponential number of them, then a TMS is probably inappropriate. Furthermore, connecting a TMS to a PROLOG interpreter is a bad idea for most applications. All the advantages of a TMS notwithstanding, on average it will substantially degrade PROLOG performance. Of course, one can find PROLOG programs that would perform better with a TMS, but such examples are rare. *(p.175)*

- **When TMSs help with OPS-like systems**: The second area in which TMSs can hurt arises because they enforce a very rigid form of rationality: a fact cannot be removed from a context if it has a perfectly good justification for being there. TMSs only remove belief in facts that no longer have a good justification. Therefore, to retract a fact, one must somehow defeat its justifications. This discipline makes it relatively difficult to implement systems written in an OPS-like fashion which frequently and arbitrarily retract facts. Although OPS-like retraction can be implemented indirectly using a TMS, the result is relatively inefficient because TMSs keep all old inactive justifications around in case their antecedents become believed again. Thus the problem solver's database becomes filled with justifications and facts which have become irrelevant. *(p.175)*

- **JTMS is logically very weak**: The JTMS is logically very weak as it only allows definite clauses. Consequently, a JTMS can never deduce the negation of any node. A node being out can mean either that its negation is propositionally derivable from A and J or that neither it nor its negation is derivable. *(p.190)*

- **In/Out vs True/False**: Many inference engines need to reason about the negation of data. This is achieved by creating two nodes for a datum: a node representing the datum and a node representing its negation. Since a node can be in or out, this gives four possible combinations for believing in the datum. By encoding these as two nodes the inference engine is essentially functioning with a four-valued logic: (1) P and ~P can both be in, allowing the inference engine to reason with contradictory data; (2) P is in and ~P is out, which can be interpreted as P being true; (3) P is out and ~P is in, which can be interpreted as P being false; (4) both P and ~P are out, indicating that nothing is known about P. *(p.190-191)*

- **JTMS justifications as definite clauses**: As far as the JTMS is concerned, a contradictory node is simply a node with the property that if it becomes in, the inference engine must be signaled (discussed later). It is the inference engine's job to retract assumptions so that these justifications no longer support the contradiction. A JTMS cannot represent the contradiction n as simply as ~n because ~n is a non-definite singleton clause. Extending JTMS to handle these would require a more complex propagation algorithm that propagated labels backward as well as forward through justifications. *(p.189)*

- **Stack-based context mechanism shortcomings**: (1) It has certain important disadvantages including no way of caching the results of rule firings. (2) The choice of a stack limits us to depth-first search, and other search strategies would require substantial changes. (3) It provides no means for caching the results of rule firings -- meaning the system must redefine what follows from an assumption each time it is reinstated. *(p.160)*

- **Three basic JTMS algorithm approaches**: Most JTMS implementations take the labeling approach. An example of the extent approach is the stack-oriented context mechanism of FTRE. It has the disadvantage of potentially having to manipulate large sets of nodes. The advantage of the labeling approach over the lazy approach is very clear. The labeling approach is based on the observation of the typical patterns of interaction with inference engines. Usually the ratio of queries to assumption retractions is extremely high. If this were not the case, the lazy approach would be better because it does not have to update the belief status of every node after every change. *(p.198)*

## Design Decisions and Rationale

- **Why use FTRE for N-queens**: The choice of a stack organization for logical environments favors a control strategy of depth-first search. Sometimes other search strategies are desirable, such as best-first search or beam search. *(p.165)*

- **Stack-based context leads to TMS invention**: The motivations analyzed in section 6.1 guided the invention of truth maintenance systems in the mid 1970s. TMSs have become a common and very widely used piece of AI technology. They are used in qualitative simulation, analog circuit design and analysis systems, temporal reasoning, diagnostics, ITS systems, knowledge representation languages, rule-based deduction systems, and constraint languages, as well as in many commercially available expert system shells such as KEE and ART. *(p.173)*

- **JTMS vs ATMS vs LTMS vs CMS selection**: In this book we focus on the JTMS, LTMS, and ATMS. For this introductory book, the complexity of the NMJTMS and CMS outweighs their utility. In choosing a TMS to use, the first principle is that one should use the simplest TMS that naturally matches the task. The reason for this is that the more complex TMSs are likely to spend a great deal of computation on aspects irrelevant to the task. The list of TMSs above is organized roughly in order of complexity. *(p.185)*

- **Two fundamental criteria for TMS selection**: (1) Whether the task requires finding most or all solutions, and (2) whether the number of context changes is far greater than the number of queries about node labels. *(p.185)*

- **Why separate inference engine from TMS**: All communication between the inference engine and the TMS is in terms of these nodes. It is important to note that the TMS and the inference engine interpret the nodes quite differently. To the problem solver the nodes represent data from which it can draw inferences. For example, suppose node R0001 has as its datum the inference engine rule, and node R0002 has as its datum the assertion `(Graduate-Student Robbie)`. The TMS cannot directly make these deductions as it may not examine the datum of a node. However, if the inference engine informs the TMS that R0003 follows from the conjunction of R0001 and R0002, then it can. *(p.176)*

- **Contradiction handling strategy**: When trying to detect contradictions and you find an assertion is negative, you believe the positive term too. Then you have a contradiction. Notice we must put the trigger for `(assert! ?p)` before the trigger for `?p` in order for the class of `?p` to be bound when spawning the compiler's internment. Actually this is a good idea in any case: typically there will be many more positive terms than negative ones. Control assertions, for instance, are positive terms whose negations make little sense, so spawning rules to look for their negations is a gross waste of resources. *(p.157)*

## Figures of Interest

- **Figure 5.3**: The N-queens puzzle. Shows a good solution and bad solution on a chess board for 4-queens. *(p.152)*

- **Figure 5.4**: Performance as N increases. Two charts: (1) Time in seconds for N-Queens using JTRE -- grows exponentially from ~0.5s at N=4 to ~95s at N=8. (2) Number of Assumptions for N-Queens using FTRE -- also grows exponentially from ~66 at N=4 to ~15000 at N=8. *(p.156)*

- **Table 5.1**: Performance of FTRE on N queens. Tabular data showing N, # rules, # Assumptions, and Time for N=4 through N=8. *(p.154)*

- **Figure 6.1**: The search space is characterized by a sequence of three choices -- A or B, C or D, E or F. A and C cannot hold together, neither can B and E. *(p.170)*

- **Figure 6.2**: Chronological backtracking leads to wasted computation. Suppose D and F taken together cause a lot of work. All this work will be lost when the context is popped. *(p.171)*

- **Figure 6.3**: Chronological backtracking leads to rediscovering contradictions. Trying B with E is always a waste of effort, since B and E taken together are contradictory. *(p.171)*

- **Figure 6.4**: Guiding backtracking. Recall that A and C are contradictory. *(p.172)*

- **Figure 6.5**: Problem solver = inference engine + TMS. Simple box diagram showing inference engine connected to TMS. *(p.174)*

- **Figure 6.6**: Dependency networks: Graphical notation. Shows eight distinct types of nodes: justification, assumption node (enabled), assumption node (retracted), simple node, premise node, contradiction node. Nodes which are both premises and contradictions are represented by superimposing their icons. *(p.178)*

- **Figure 6.7**: Dependency network structure. Shows a node DATUM with JUSTIFICATIONS above and CONSEQUENCES below. *(p.178)*

- **Figure 6.10**: Families of TMSs. A 2D classification grid: Label dimension (Simple vs Complex) x Constraint dimension (Horn/definite vs Clause). JTMS = Simple+Horn, ATMS = Complex+Horn, LTMS = Simple+Clause, CMS = Complex+Clause. Also includes NMJTMS (non-monotonic justification-based) outside the main classification. *(p.184)*

- **Figure 7.1**: Representing a node and its negation. Shows node P with In/Out states and ~P with In/Out states, yielding four combinations: P true (P in, ~P out), Contradiction (P in, ~P in), P false (P out, ~P in), Don't know (P out, ~P out). *(p.191)*

- **Figure 7.2**: Maintaining a cache. Three diagrams (a), (b), (c) showing JTMS dependency network fragments and how the FEA program interacts with the JTMS to cache results. *(p.192)*

- **Figure 7.3**: Maintaining the semantics of default C. Shows dependency structures for retracting assumption B and the resulting structures. *(p.193)*

- **Figure 7.4**: Dependency network just before and after enabling assumption A. Shows nodes with in/out labels transitioning when assumption A is enabled. *(p.199)*

## Implementation Notes

- **`generate-unify-tests` procedure**: Ensures that the same value appears everywhere the variable is used in the assertion. If it isn't, then in fact the pattern does not match. The procedure `generate-pattern-tests` creates an appropriate list of such tests. The final use of these expressions is implementing the :TEST option. Note that the test, if any, is passed as the argument `extra-test` to `generate-match-body`. The code of the :TEST is transformed into an executable expression by the call to `nsublis`. For increased robustness we ensure that no free pattern variables appear in the test, signaling an error if any are found. The resulting code is then appended to the end of the unification tests, thus ensuring the pattern is correct before attempting the final check. *(p.150)*

- **`consp test` generation**: What happens when a variable is encountered in `generate-unify-tests` depends on whether or not it was previously bound. If this trigger is what binds it (as indicated by its appearance in vars) then the current path is pushed onto the list of specifications of where that variable has appeared. If the variable is bound, an equality test is added to ensure that the value in the run-time environment is the same as that to which unifying the pattern with this assertion would bind it. *(p.151)*

- **FTRE implementation details for KM***: In Chapter 4 we saw that the inference rules of a natural deduction system could be easily mapped into the rules of a pattern-directed inference system. However, we could not completely implement KM* since we had no mechanism for manipulating assumptions. Our new context mechanism allows us to complete the implementation of KM*. Recall that three inference rules require temporary assumptions: INDIRECT PROOF, NOT INTRODUCTION, and CONDITIONAL INTRODUCTION. *(p.155)*

- **Control predicates `premise(P)` and `goal(P)`**: `premise(x) is true just in case (x) is believed as an assumption of the problem`. Similarly, goal can be defined: `goal(x) is true just in case (x) is the proposition whose proof is the goal of our goal`. Instead of asserting `show(P)` in our problems statement to indicate our goal, we would instead `assert! goal(P)`. Adding these control predicates can enhance our problem solver's performance. *(p.164)*

- **Contradiction handler in JTMS**: When the JTMS detects that the current database state contains contradictions, it calls the inference-engine-supplied contradiction handling procedure with two arguments: the particular instance of the JTMS involved and the current set of contradictory nodes. The JTMS checks for contradictions as the very last thing it does. Therefore, it is acceptable (and usual) for the contradiction handler to simply do a throw. Also, the contradiction handler can perform any JTMS operations it likes. If the contradiction handler does nothing, the JTMS procedure originally invoked by the inference engine just returns, leaving contradictions believed in the database. *(p.197)*

- **TMS-inference engine basic transactions**: (1) Upon request, the TMS creates a node with the specified properties. (2) The TMS accepts records of inference engine deductions (usually the justifications). (3) The TMS maintains the correct labels for nodes and supplies them on request. (4) When a contradiction is detected, the inference engine is signaled. (5) The TMS accepts rules from the inference engine to be scheduled for execution when particular belief conditions are met. *(p.180)*

## Quotes Worth Preserving

- "The ability to hypothetically add facts to the database which may later be retracted is fundamentally important in problem solving. The core reason for this is that most AI tasks cannot be solved in the simple autocrat-driven way that IRE uses. Sophisticated problem solvers are often faced with having to choose among mutually incompatible choices with no immediate reason to choose one over the other. To make progress, some choice must be made which may later have to be retracted. The context mechanism of FTRE achieves some of the desired functionality, and schemes like it were used in many early AI problem solvers. Unfortunately, the context mechanisms also suffer from many shortcomings. Truth maintenance systems are a general problem-solving facility to help inference engines such as IRE and FTRE conveniently and efficiently manipulate assumptions." *(p.167-168)*

- "A problem solver must identify responsibility for its conclusions by providing rational explanations of how its conclusions follow from the premises. Generally, just providing the answer is not enough. A medical diagnosis program that suggests cutting a patient's heart out would not be listened to unless it explained why. If, for instance, radical bypass surgery is necessary for a particular set of reasons, then the user can look at those reasons and see if they make sense." *(p.168)*

- "In an ideal world all the data would be valid and every constraint imposed would be perfectly satisfied. Neither we nor our programs live in such a world. For example, the data we give our programs can be wrong. If a data entry person or machine misreports the patient's temperature as 986 degrees, then our diagnosis program should not call the fire department." *(p.168)*

- "Futile backtracking was the catalyst for the invention of truth maintenance systems. Suppose the search (see Figure 6.4) detected an inconsistency while exploring the solution {A,C,F}. This inconsistency is caused by the fact that choices A and C are incompatible with each other. Chronological backtracking, as the name implies, backtracks to the most recent choice the search has made and explores the next alternative, namely F. Exploring {A,C,F} is futile because it too contains the contradictory pair A and C. If somehow we could detect the choices an inconsistency depended on, then the backtracker could backtrack to the most recent choice contributing to the contradiction. This strategy, called dependency-directed backtracking, is made possible by truth maintenance systems." *(p.172)*
