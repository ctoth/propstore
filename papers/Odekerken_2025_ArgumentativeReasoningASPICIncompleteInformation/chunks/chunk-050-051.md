# Pages 050-051 Notes

## Chapters/Sections Covered
- Additional empirical tables
- Appendix / remaining hardness proofs

## Key Findings
- The appendix extends the single-query empirical picture to `blocked`, `out`, and `unsatisfiable` relevance, with separate solved-count/runtime tables. *(p.50-51)*
- The remaining pages continue the hardness-proof machinery for the relevance theorem and support the stated `Sigma_2^P` bounds. *(p.46-49, p.50-51)*

## Equations Found (LaTeX)
- No new central modeling equations beyond the appendix proof constructions. *(p.50-51)*

## Parameters Found (table)
- Additional runtime tables for `blocked`, `out`, and `unsatisfiable` relevance under the same benchmark regime. *(p.51)*

## Rules/Algorithms
- No new algorithms; these pages support the main theorem proofs and supplementary empirical reporting. *(p.46-51)*

## Figures of Interest
- Tables 4-6: appendix empirical tables for non-`defended` relevance statuses. *(p.50-51)*

## Quotes Worth Preserving
- The appendix exists to complete both the proof obligations and the status-specific relevance results, not to alter the main modeling story. *(p.46-51)*

## Implementation Notes
- The main implementation burden is already in the body of the paper; the appendix mostly confirms that the same machinery extends across all four statuses. *(p.46-51)*
